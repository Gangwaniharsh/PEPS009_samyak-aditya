# Async User Fetch 👤

**Goal:** Fetch user data from a public API and display it on the page.

## 📝 Concepts Covered
- `fetch()` API
- `async` / `await`
- DOM Manipulation
- Error Handling (`try...catch`)

## 🎯 Task Checklist
- [ ] Create an `async` function `fetchUser()`.
- [ ] Fetch data from: `https://jsonplaceholder.typicode.com/users/1`.
- [ ] Display the User's **Name**, **Email**, and **City** in the HTML.
- [ ] Handle errors (e.g., if the user doesn't exist).

## 🚀 Bonus
- Add a button "Get Random User" handling IDs from 1 to 10.
